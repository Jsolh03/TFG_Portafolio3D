/**
 * Imágenes de los usuarios destacados.
 *
 * KHALED:
 * El archivo `khaled.png` debe vivir en la carpeta `public/` de la raíz del proyecto
 * (la misma donde está `favicon.svg`). Vite lo sirve directamente bajo la URL `/khaled.png`.
 *
 * Para cambiar la imagen sin tocar código: reemplaza el archivo PNG.
 * Para usar una URL externa: cambia el valor de `khaled` por la URL completa.
 */
export const USER_PHOTOS = {
  khaled: '/khaled.png',
  laura: 'https://i.pinimg.com/736x/f3/8f/70/f38f7066edb3f22c64e57faa320abad5.jpg'
};
